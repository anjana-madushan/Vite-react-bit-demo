---
description: 'A react_vite_app app.'
labels: ['app', 'react']
---

A React application for react_vite_app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use react_vite_app 
```

To run the app locally, run the following:

```
bit run react_vite_app 
```
